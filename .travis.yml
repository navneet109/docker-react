sudo: required
language: generic 

services:
  - docker

before_install:
  - docker build -t navneet109/docker-react -f Dockerfile.dev .  
script:
  - docker run -e CI=true navneet109/docker-react npm run test